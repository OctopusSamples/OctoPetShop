apiVersion: v1
kind: Service
metadata:
  name: octopetshop-productservice-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: productservice
  ports:
    - port: 80
      targetPort: 80
      name: http-port
    - port: 443
      targetPort: 443
      name: https-port