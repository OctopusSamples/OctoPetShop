name: Action Tester            

on:
  push:
    branches: [ demo ]
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: twerthi/Octopus-Deploy-Action@alpha1
      with:
        command: "pack" 
